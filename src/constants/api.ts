// Backend URL.
// Production (deployed on Render) — used by the shared APK build:
export const API_URL = "https://ai-workout-backend-xqhz.onrender.com";

// For local development, temporarily swap to one of these instead:
// - iOS Simulator on same Mac:      http://localhost:3000
// - Physical phone on same WiFi:    http://YOUR_MAC_LOCAL_IP:3000  (ipconfig getifaddr en0)
